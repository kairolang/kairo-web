---
title: std::Memory::in_rotdata
description: Checks if a pointer is in the ROTData memory region
---

# in_rotdata

Checks if a pointer is in the ROTData memory region

## Signature

```kairo
in_rotdata(ptr: *const void) -> bool
```



