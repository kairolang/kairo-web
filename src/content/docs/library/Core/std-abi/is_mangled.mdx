---
title: std::abi::is_mangled
description: Checks if a string is a mangled name and returns the detected object type
---

# is_mangled

Checks if a string is a mangled name and returns the detected object type

## Signature

```kairo
is_mangled(input: const &string) -> ObjectType
```



