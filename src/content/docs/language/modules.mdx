---
order: 21
title: Modules
description: put something here later
---

`::` is scope resolution. Used for namespace access (`std::println`), static members of classes/structs, enum variants (`MyEnum::Variant`), and module paths.
